export const HOST_IP = '192.168.1.137';
export const HOST = `http://${HOST_IP}:4000`; // this can't be localhost, has to be IP of the machine
export const ENDPOINTS = {
  heroes: '/heroes',
  types: '/types',
  avatars: '/avatars',
};
